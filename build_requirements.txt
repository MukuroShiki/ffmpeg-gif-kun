# FFmpeg GUI Kun - Build Requirements
# PyInstaller用の追加依存関係

# 標準ライブラリのみ (main requirements)
# No additional dependencies required for basic functionality

# Build dependencies
pyinstaller>=5.0.0
setuptools>=65.0.0

# Optional: Enhanced drag-and-drop support
# tkinterdnd2>=0.3.0
